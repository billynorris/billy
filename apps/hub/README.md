# hub

The landing page served at each domain apex (`billynorris.co.uk`,
`billynorris.gay`). Static only — no API. Lists the platform's apps and links to
them on the current TLD (so links from `.gay` stay on `.gay` and keep the pride
theme). `@billynorris/hub-web`, React + Vite.
