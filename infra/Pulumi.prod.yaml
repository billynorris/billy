config:
  aws:region: eu-west-2
  # Root domains the platform serves. Each gets apex + wildcard coverage on the
  # single CloudFront distribution and ACM certificate. Adding a domain here is
  # all that's needed to extend coverage.
  billy:rootDomains:
    - billynorris.co.uk
    - billynorris.gay
